package Main;

import Testeo.TestDijkstra;

/**
 * Clase principal que sirve como punto de entrada del programa para ejecutar
 * los tests de la implementación de grafos ponderados.
 */
public class Main {

    /**
     * Método principal que se ejecuta al iniciar el programa.
     * Llama a {@link TestDijkstra} para realizar pruebas del algoritmo de Dijkstra.
     *
     * @param args argumentos de línea de comandos (no utilizados)
     */
    public static void main(String[] args) {
        TestDijkstra.main(args);
    }
}

